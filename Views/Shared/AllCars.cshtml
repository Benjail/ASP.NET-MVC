@model ASP.Data.Models.Car
<div class="col-lg-3">
    <img class="img-thumbnail" src="@Model.Img" alt="Изображение не доступно">
    <h2>@Model.Name</h2>
    <h5>@Model.Category.CategoryName</h5>
    <p> @Model.Price.ToString("c")</p>
    <a class="btn btn-warning" href="#">Подробнее</a> 
</div>

